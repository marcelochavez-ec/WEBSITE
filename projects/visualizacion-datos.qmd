---
title: "Visualización de Datos con Python"
date: today
format: 
  html:
    toc: true
    code-fold: true
    code-summary: "Mostrar código"
    theme: cosmo
jupyter: python3
execute:
  echo: true
  warning: false
  message: false
  error: true
---

## Estructura de un esquema de gráficos con Matplotlib

<img src="ESTRUCTURA_MATPLOTLIB.png" style="height: 50%; width: 100%; object-fit: contain" onclick="window.open('https://arxiv.org/abs/2210.07278', 'blank');"/>

<a href="https://arxiv.org/abs/2210.07278" target="_blank">arXiv Preprint</a> | <a href="https://github.com/marvinschmitt/MetaUncertaintyPaper" target="_blank">Code</a><br>

# Introducción

El presente es un Jupyter Notebook que demuestra gráficos utilizando la gramática de gráficos con Python, iniciaremos con la librería **Matplotlib**

## Operaciones básicas

```{python}
#| echo: false
import os

# Cambia el directorio de trabajo
os.chdir("D:/DATA_SCIENCE_ENDI/NOTEBOOKS_R/MULTIVARIANTE/MARCELO_WEBSITE/docs/projects/")

```


```{python}
import pandas as pd

df_ipc = pd.read_excel("IPC/SERIE HISTORICA IPC_05_2025.xls",
                        sheet_name="2. VARIACIÓN MENSUAL",
                        skiprows=5,
                        engine="xlrd",
                        nrows=57)
# Lista con los nombres correctos de las columnas
nuevos_nombres = ["AÑOS",
                  "ENERO", 
                  "FEBRERO",
                  "MARZO",
                  "ABRIL",
                  "MAYO",
                  "JUNIO",
                  "JULIO",
                  "AGOSTO", 
                  "SEPTIEMBRE", 
                  "OCTUBRE",
                  "NOVIEMBRE",
                  "DICIEMBRE",
                  "PROMEDIO_MENSUAL"]

# Asignar los nuevos nombres al DataFrame
df_ipc.columns = nuevos_nombres

display(df_ipc)
print("TIPO DE VARIABLES:")
df_ipc.dtypes
```

```{python}
# Lista de meses en orden
meses = ['ENERO', 'FEBRERO', 'MARZO', 'ABRIL', 'MAYO', 'JUNIO',
         'JULIO', 'AGOSTO', 'SEPTIEMBRE', 'OCTUBRE', 'NOVIEMBRE', 'DICIEMBRE']

# 1. Convertir de ancho a largo
df_long = df_ipc.melt(id_vars='AÑOS', value_vars=meses,
                  var_name='MES', value_name='VALOR')

# 2. Crear un orden cronológico para los meses
orden_meses = {mes: i+1 for i, mes in enumerate(meses)}
df_long['MES_NUM'] = df_long['MES'].map(orden_meses)

# 3. Ordenar por año y mes
df_long = df_long.sort_values(by=['AÑOS', 'MES_NUM']).reset_index(drop=True)

# 4. Calcular variación absoluta y porcentual respecto al mes anterior
df_long['VARIACION'] = df_long['VALOR'].diff()
df_long['VARIACION_PORCENTUAL'] = df_long['VARIACION'] / df_long['VALOR'].shift(1) * 100
display(df_long)
```

----

## Apps en Shiny:

### **ENEMDU**

::: {.embed-container}
<iframe 
  src="https://marcelo-chavez.shinyapps.io/ENEMDU_VISOR/" 
  width="100%" 
  style="border: none;">
</iframe>
:::







